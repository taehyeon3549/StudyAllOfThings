{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Compile Markdown",
        "type": "shell",
        "command": "markdown-it ${fileDirname}/${fileBasename} -o ${fileDirname}/${fileBasename}.html",
        "problemMatcher": [],
        "group": {
            "kind": "build",
            "isDefault": true
        }
      }
    ]
  }